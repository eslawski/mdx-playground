const colors = {
  green: "#308014",
  red: "#8B0000"
}

const breakpoints = {
  tablet: '1200px',
  phone: '600px',
}

const blog = {
  inlineMediaWidth: "70%",
  inlineMediaWidthSmall: "100%",
}

const maxWidths = {
  index: "1000px",
  post: "800px",
  imagesSection: "1000px"
}

const theme = {
  colors,
  breakpoints,
  blog,
  maxWidths,
  headerHeight: "50px",
}

export default theme