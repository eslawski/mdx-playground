const colors = {}

const breakpoints = {
  tablet: '1200px',
  phone: '600px',
}

const blog = {
  inlineMediaWidth: "70%",
  inlineMediaWidthSmall: "100%",
}

const maxWidths = {
  index: "800px",
  post: "800px",
  imagesSection: "1000px"
}

const theme = {
  colors,
  breakpoints,
  blog,
  maxWidths,
  headerHeight: "50px",
}

export default theme